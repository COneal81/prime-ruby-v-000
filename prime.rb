def prime?(num)
  num = gets.to_i
  last = gets.to_i
 while (num <= last)
  prime_flag = TRUE
  
  x = 2 
  
  while (x <= num / 2)
  
 
  if num == prime?
    return TRUE
  else
    return FALSE
end
end
